def NWD(a,b):
    while a!=b:
        if a>b:
            a=a-b
        else:
            b=b-a
    return a

def main():
    x=int(input("x= "))
    y=int(input("y= "))
    a=NWD(x, y)
    print(a)

if __name__ == '__main__':
    main()
